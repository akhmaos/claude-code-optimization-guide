# Minimal CLAUDE.md Template

Use this for small projects with a simple structure.

---

```markdown
# Project Name

One-line description of what this project does.

## Stack
- Language: [e.g., TypeScript, Python, Go]
- Framework: [e.g., Express, FastAPI, Gin]
- Database: [e.g., PostgreSQL, SQLite]

## Commands
- `npm test` — run tests
- `npm run dev` — start dev server
- `npm run build` — production build

## Structure
- `src/` — source code
- `tests/` — test files
- `config/` — configuration

## Notes
- [Any non-obvious conventions or gotchas]
```

---

**Token cost**: ~50-80 tokens per session (loaded automatically, cached after first call).
